// Initialise all providers on first import
import './providers'

export { mediaRegistry } from './registry'
export type { MediaItem, MediaProvider, MediaType, SearchOptions, SearchResult } from './types'
